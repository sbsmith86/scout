'use strict';

// Proposal and outreach message drafters.
// See CLAUDE.md for draft output types (full proposal, LOI, warm outreach).

const { exportDraft } = require('./export');

module.exports = { exportDraft };
