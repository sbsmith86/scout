node_modules/
.env
dist/
build/
*.log
.DS_Store

# Real profile contains NDA-protected data — use profile.example.json as template
config/profile.json

# Google service account credentials
config/google-service-account.json
*.json.key

# Runtime state — tracks last-successful-fetch timestamps per source plugin
config/source-runs.json
