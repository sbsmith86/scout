Add an on-demand triggering mechanism to the pipeline. The mechanism would allow the pipeline to be run at any given time, not just on the weekly cron job. For example, introduce a CLI command or an API endpoint to enable triggering on-demand.